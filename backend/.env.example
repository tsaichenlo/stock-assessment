# Copy to .env for local overrides. All values have defaults; none are required.

# Port the API listens on.
PORT=3000

# Browser origin allowed to call this API (the frontend dev server).
CORS_ORIGIN=http://localhost:5173
